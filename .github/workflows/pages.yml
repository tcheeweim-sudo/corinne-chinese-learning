name: Deploy static PWA to GitHub Pages

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: pages-${{ github.ref }}
  cancel-in-progress: false

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - run: node tests/check.cjs

  deploy:
    if: github.ref == 'refs/heads/main' && github.event_name != 'pull_request'
    needs: validate
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pages: write
      id-token: write
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/configure-pages@v5
      - name: Stage only public app files
        run: |
          mkdir _site
          cp index.html styles.css app.js audio.js mission.js practice.js revision.js rewards.js progress.js freewrite.js manifest.webmanifest sw.js THIRD_PARTY_NOTICES.md _site/
          cp -R icons vendor character-data curriculum shop tiger audio _site/
          mkdir -p _site/assets/tiger/poses _site/assets/tiger/house _site/assets/tiger/items _site/assets/tiger/icons
          cp assets/tiger/poses/*.png _site/assets/tiger/poses/
          cp assets/tiger/house/house-room-empty.png _site/assets/tiger/house/
          cp assets/tiger/items/house-*.png assets/tiger/items/tiger-ribbon.png assets/tiger/items/tiger-hat.png _site/assets/tiger/items/
          cp assets/tiger/icons/coin-icon.png assets/tiger/icons/badge-star.png _site/assets/tiger/icons/
          touch _site/.nojekyll
      - uses: actions/upload-pages-artifact@v3
        with:
          path: _site
      - name: Deploy
        id: deployment
        uses: actions/deploy-pages@v4
